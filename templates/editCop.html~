

    <div class="grid_7 alpha incident_form">
      <form action="" method="post" enctype="multipart/form-data">
          {% csrf_token %}
					{% if badge %}
          <input id="id_badge" type="text" maxlength="6" value="{{badge}}" name="badge">
          {% else %}
          {{form.badge}}
          {% endif %}
          {{form.force}}
          {{form.name}}
          
          <div class="grid_7 input alpha omega">
            <input type="Button" value="Save" onClick="saveCop()"/>
          </div>
      </form>
    </div>


